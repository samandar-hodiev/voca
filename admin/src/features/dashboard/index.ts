// dashboard: the feature's public surface.
//
// Other features import from here, never from a file deep inside this folder. Keeping one
// entry point is what makes a feature removable without hunting down stray imports.
