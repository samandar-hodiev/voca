# Content seed loader.
#
# Intended behaviour: apply backend/migrations/seed/*.sql to load categories and words
# (text, IPA, target phonemes, meanings, example sentences, audio URLs).
#
# Content is never hardcoded into the Flutter app — ARCHITECTURE.md §13.4.
