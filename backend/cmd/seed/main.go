// Content seed loader.
//
// Loads categories and words (text, IPA, target phonemes, meaning, example sentence, audio
// URL) from backend/migrations/seed into the target environment.
//
// Seed data is reviewed like code and is the MVP substitute for a content CMS — an admin
// tool is deliberately deferred (ARCHITECTURE.md 13.4, 35).

package main
