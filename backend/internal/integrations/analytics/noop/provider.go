// integrations/analytics/noop: analytics implementation that does nothing.
//
// The CI default (ANALYTICS_PROVIDER=noop) and the switch for any environment where
// tracking must be off.

package noop
