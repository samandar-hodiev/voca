// user: route registration on the /api/v1 router group.
//
// Declares which routes are public, which require RequireAuth, and which require
// RequirePremium or a usage-limit check.
//
// See ARCHITECTURE.md 11.2 (endpoint contracts) and 30 (endpoint map).

package user
