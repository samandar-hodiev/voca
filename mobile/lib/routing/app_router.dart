// routing: the GoRouter instance.
//
// Routes are composed from per-feature fragments so adding a feature costs one line here,
// not an edit threaded through a large file.
//
// Deep links are first-class (voca://practice/daily, notification taps, subscription
// return), because every push notification must land on the exact screen it promises
// (ARCHITECTURE.md 4.4, 17.3).
