// pronunciation/data: multipart upload of the recording plus reference text. Sends declared audio_format and duration_ms so the server never has to guess (ARCHITECTURE.md 12.2).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
