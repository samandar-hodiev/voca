// pronunciation/data: result DTO plus toDomain mapping. Must tolerate unknown enum values and unexpected fields, because the API contract is additive-only and old app versions must keep working (ADR-009).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
