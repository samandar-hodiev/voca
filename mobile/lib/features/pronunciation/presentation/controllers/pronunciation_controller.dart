// pronunciation/presentation: assessment state — idle, uploading, assessing, result, failed. The assessing state is genuinely long, so it is an explicit state rather than an ad-hoc boolean (ARCHITECTURE.md 4.3).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
