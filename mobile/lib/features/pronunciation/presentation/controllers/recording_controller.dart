// pronunciation/presentation: recording state — idle, permission denied, recording, too short, ready. Enforces the capture spec from core/audio/audio_recorder.
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
