// progress/domain: ranked weak sounds from GET /api/v1/progress/weak-sounds. Ships after MVP, but the phoneme data behind it is captured from the very first attempt — history cannot be created retroactively (ARCHITECTURE.md 15.1).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
