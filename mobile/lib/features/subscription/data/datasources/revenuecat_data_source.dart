// subscription/data: RevenueCat SDK wrapper — offerings, purchase, restore, and identifying the customer with OUR user ID. Uses the PUBLIC SDK key only; the secret key is backend-only (ARCHITECTURE.md 9.3, 25.2).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
