// subscription/domain: Entitlement — tier, status, expiry, trial state, and remaining daily quota. A UI HINT ONLY: the backend is the sole authority (ARCHITECTURE.md 9.4).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
