// subscription/domain: runs the native purchase through the RevenueCat SDK, then asks the backend to re-read entitlement.
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
