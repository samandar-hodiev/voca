// subscription/domain: reads current entitlement from GET /api/v1/subscriptions/me — the server's answer, not a locally cached flag.
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
