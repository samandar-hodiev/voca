// settings/domain: saves preferences via PUT /api/v1/users/me/preferences. Notification opt-ins are enforced SERVER-SIDE, not just hidden in the UI (ARCHITECTURE.md 17.3).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
