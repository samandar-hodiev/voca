// settings/domain: UserPreferences — UI language, learning language, accent, difficulty, daily goal, timezone, reminder settings. Several fields currently hold exactly one possible value; they exist now so adding a second costs no migration (ARCHITECTURE.md 8.4).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
