// practice/domain: Word entity — text, IPA, meaning, example sentence, audio URL, difficulty. Content comes from the API and is NEVER hardcoded in the app (ARCHITECTURE.md 13.4).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
