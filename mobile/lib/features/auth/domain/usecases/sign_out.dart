// auth/domain: Sign out — revokes the refresh token server-side and clears secure storage and caches (ARCHITECTURE.md 8.3).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
