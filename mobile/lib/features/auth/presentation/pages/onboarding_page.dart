// auth/presentation: onboarding — collects learning preferences (difficulty, daily goal) that seed user_preferences.
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
