// auth/presentation: Apple and Google buttons, styled to each platform's brand requirements (a store review item).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
