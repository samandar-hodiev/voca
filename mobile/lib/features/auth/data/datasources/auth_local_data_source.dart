// auth/data: reads and writes tokens through core/storage/secure_storage. Keychain and Keystore only — never SharedPreferences (ARCHITECTURE.md 18.2).
//
// Layer rules: ARCHITECTURE.md 4.2 (what belongs in each layer), 31.1 (dependencies).
