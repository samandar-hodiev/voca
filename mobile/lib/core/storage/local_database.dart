// core/storage: structured local cache (Drift or Isar) for word lists and last-known
// progress.
//
// Offline policy: browse cached words and view cached progress offline; ASSESSMENT REQUIRES
// CONNECTIVITY, because it inherently needs the server. A clear "needs internet" state beats
// a silent failure (ARCHITECTURE.md 4.5).
