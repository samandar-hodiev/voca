// core/storage: flutter_secure_storage wrapper — Keychain on iOS, EncryptedSharedPreferences
// on Android.
//
// TOKENS ONLY. Access and refresh tokens must never live in SharedPreferences, a plain file,
// or a log line (ARCHITECTURE.md 4.5, 18.2).
