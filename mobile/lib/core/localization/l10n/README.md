# ARB translation files. app_uz.arb is the first UI language; app_en.arb ships alongside it
# from day one to prove the plumbing works before a second language is real.
#
# ALL user-visible strings live here — including the text for server-provided message keys
# and for every API error code. Nothing user-facing is hardcoded in a widget.
#
# Adding a language is then a translation task, not a code change (ARCHITECTURE.md 4.6, 34).
# Translation completeness is checked in CI.
