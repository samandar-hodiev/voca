// core/theme: color tokens, including the score bands.
//
// Score band thresholds are defined SERVER-SIDE and mirrored here, so the app and backend
// never disagree about what "good" means (ARCHITECTURE.md 6.4).
