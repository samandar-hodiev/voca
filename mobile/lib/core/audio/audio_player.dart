// core/audio: playback of reference pronunciation from words.audio_url, cached on device.
//
// Reference audio is CONTENT, not user data — versioned by URL so a corrected recording
// invalidates cleanly (ARCHITECTURE.md 12.4).
