// core/network: sends X-Request-ID, X-App-Version and X-Platform, and retains the request ID
// from the response so the error UI can display it. A user's screenshot then maps directly
// to a backend log line (ARCHITECTURE.md 18.4).
